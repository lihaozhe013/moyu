appId: com.moyu.professionalcanvas
productName: Professional Canvas
copyright: Copyright © 2026 Professional Canvas contributors
directories:
  output: dist
  buildResources: build
files:
  - out/**
  - package.json
  - '!**/*.map'
asar: true
icon: build/icon
mac:
  artifactName: Professional-Canvas-mac-${arch}-${version}.${ext}
  category: public.app-category.graphics-design
  hardenedRuntime: true
  target:
    - dmg
    - zip
win:
  executableName: professional-canvas
  target:
    - target: nsis
      arch:
        - x64
    - target: portable
      arch:
        - x64
  artifactName: Professional-Canvas-win-${arch}-${version}.${ext}
nsis:
  artifactName: Professional-Canvas-win-${arch}-${version}-setup.${ext}
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  perMachine: false
portable:
  artifactName: Professional-Canvas-win-${arch}-${version}-portable.${ext}
dmg:
  artifactName: Professional-Canvas-mac-${arch}-${version}.${ext}
publish: null
