# Packaging assets

The checked-in icon set is a neutral geometric placeholder for the moyu shell. Replace it with approved product branding before a signed distributable release. Until signing and product approval are complete, package outputs remain unsigned development artifacts.
